---
description: An explanation of vertex colors in three.js.
title: Vertex Colors
---

import App from "@components/vertex-colors/app.svelte";
import Tabs from "@components/tabs.astro";
import tabItems from "@components/vertex-colors/tabsItems";
import { Aside, Steps, TabItem } from "@astrojs/starlight/components";

Assigns a color to each vertex of a geometry. Colors are interpolated along the face just like any other vertex attribute.

<Aside>
	Note that most three.js materials support vertex colors through the
	**vertexColors** prop.
</Aside>

## Usage

{/* prettier-ignore-start */}

<Steps>
1. Assign a **"color"** attribute to the buffer geometry.

    ```ts
    geometry.setAttribute("color", colorAttribute);
    ```

2. Set the **vertexColors** property of the material to **true**.

    ```ts
    const material = new MeshBasicMaterial({
    	vertexColors: true,
    });
    ```
</Steps>

{/* prettier-ignore-end */}

## Example

<App client:visible />

<Tabs items={tabItems} />
